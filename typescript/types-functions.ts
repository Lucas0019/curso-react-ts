// Tipando em função - tipamos os paramentros
function soma(n1: number, n2: number) {
  return n1 + n2;
}

const result = soma(1, 2); // 3

// Tipo void (vazio)
function noReturn() :void {
  return  undefined;
}
